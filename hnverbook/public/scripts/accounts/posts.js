const postContainer = document.getElementById('screen');
postContainer.addEventListener('scroll', (e) => {
   if (postContainer.scrollTop + postContainer.clientHeight >= postContainer.scrollHeight) {
     
   }
})